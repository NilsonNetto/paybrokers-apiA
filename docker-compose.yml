version: '3.1'

services:

  mongo:
    image: mongodb/mongodb-community-server
    restart: always
    ports:
      - 17017:27017

  mongo-express:
    image: mongo-express
    restart: always
    ports:
      - 8081:8081
    environment:
      ME_CONFIG_MONGODB_URL: mongodb://mongo:27017/